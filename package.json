{
  "name": "aksharmitra-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "ai",
    "database",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend"
  },
  "description": "AksharMitra monorepo — frontend, AI, database, and backend packages"
}
